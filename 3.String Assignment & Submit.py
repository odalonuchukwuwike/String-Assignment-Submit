#Python processes white spaces and we need to know there uses if the have, if the dont then we just remove them

some_string = (" Removing white spaces from the start and end of a string or text. ");

some_string.rstrip();   #removing the right white space

some_string.lstrip();   #removing the left white space
print(some_string);